version: '3.8'

# ──────────────────────────────────────────────────────────────────────────────
#  Self-hosted Replit replacement — 192.168.1.168
#
#  Port map:
#    3225  VPNWatch  — Palo Alto VPN monitor
#    7080  Coder     — browser IDE + workspace orchestrator  ← main
# ──────────────────────────────────────────────────────────────────────────────

services:

  # ── PostgreSQL — shared database (:5432) ──────────────────────────────────
  postgres:
    image: postgres:16-alpine
    container_name: postgres
    restart: unless-stopped
    network_mode: host
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=postgres
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./setup/init-db.sql:/docker-entrypoint-initdb.d/init.sql:ro
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  # ── Coder — browser IDE + workspace orchestrator (:7080) ──────────────────
  coder:
    image: ghcr.io/coder/coder:latest
    container_name: coder
    restart: unless-stopped
    network_mode: host
    environment:
      - CODER_HTTP_ADDRESS=0.0.0.0:7080
      - CODER_ACCESS_URL=https://coder.asheindallas.com
      - CODER_WILDCARD_ACCESS_URL=*.coder.asheindallas.com
      - CODER_PG_CONNECTION_URL=postgresql://coder:coderpass@localhost:5432/coder?sslmode=disable
      - CODER_TELEMETRY_ENABLE=false
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - coder-data:/home/coder/.config
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:7080/healthz"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

  # ── VPNWatch — Palo Alto VPN monitor (:3225) ──────────────────────────────
  vpnwatch:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vpnwatch
    restart: unless-stopped
    network_mode: host
    environment:
      - NODE_ENV=production
      - PORT=3225
      - DATA_DIR=/app/data
    volumes:
      - vpnwatch-data:/app/data
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3225/api/stats', r => process.exit(r.statusCode===200?0:1))"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s

volumes:
  postgres-data:
  coder-data:
  vpnwatch-data:
